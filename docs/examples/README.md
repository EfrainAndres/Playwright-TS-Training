# Archived Learning Examples

This folder keeps earlier scratch tests and old Page Object examples for reference. They are outside the Playwright `testDir`, so they are not part of the active interview-ready suite.

Use the active tests under `tests/ui`, `tests/api`, and `tests/accessibility` when demonstrating the framework.
